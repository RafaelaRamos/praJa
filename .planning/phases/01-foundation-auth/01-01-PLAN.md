---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - lib/main.dart
  - lib/app.dart
  - lib/core/theme/app_theme.dart
  - lib/core/theme/app_colors.dart
  - lib/core/theme/app_spacing.dart
  - lib/core/theme/app_typography.dart
  - lib/core/di/injection.dart
  - lib/core/database/database_helper.dart
  - lib/core/database/migrations.dart
  - lib/core/database/seed_data.dart
  - lib/core/error/failures.dart
  - lib/core/router/app_router.dart
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "App Flutter compila e abre sem erros"
    - "SQLite abre na primeira execução e contém prestadores mockados"
    - "Tema Material 3 segue cores e tipografia do UI-SPEC"
  artifacts:
    - path: lib/core/database/database_helper.dart
      provides: SQLite singleton com migrations
      min_lines: 40
    - path: lib/core/database/seed_data.dart
      provides: Seed de prestadores mockados
      contains: insertMockProviders
    - path: lib/core/theme/app_theme.dart
      provides: ThemeData com accent #0F766E
      contains: "0xFF0F766E"
  key_links:
    - from: lib/main.dart
      to: lib/core/di/injection.dart
      via: configureDependencies() before runApp
      pattern: configureDependencies
    - from: lib/core/database/database_helper.dart
      to: lib/core/database/seed_data.dart
      via: seed on first launch
      pattern: seedMockProviders|insertMockProviders
---

<objective>
Bootstrap do projeto Flutter praJa: clean architecture scaffold, tema UI-SPEC, SQLite com seed de prestadores mockados, DI e roteamento base.

Purpose: Estabelecer fundação técnica que Plan 01-02 usa para auth e navegação.
Output: App executável com banco seeded e tema aplicado.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/SKELETON.md
@.cursor/skills/clean-architecture/SKILL.md
@.cursor/skills/flutter/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar projeto Flutter e estrutura clean architecture</name>
  <files>pubspec.yaml, analysis_options.yaml, lib/main.dart, lib/app.dart, lib/core/di/injection.dart, lib/core/error/failures.dart</files>
  <read_first>.cursor/skills/clean-architecture/SKILL.md, .cursor/skills/flutter/SKILL.md, .planning/phases/01-foundation-auth/SKELETON.md</read_first>
  <action>Executar `flutter create . --org com.praja --project-name praja` no diretório raiz (repo já tem .git). Atualizar pubspec.yaml com dependências: flutter_bloc, freezed_annotation, get_it, dartz, sqflite, path, shared_preferences, go_router, google_fonts, crypto (hash senha), intl, equatable, json_annotation. Dev: build_runner, freezed, json_serializable, flutter_test, bloc_test, mocktail. Criar estrutura: lib/core/{di,theme,database,error,router,utils}, lib/features/auth/{data,domain,presentation}, lib/features/home/{data,domain,presentation}. main.dart chama WidgetsFlutterBinding.ensureInitialized(), configureDependencies(), runApp(PraJaApp). failures.dart define CacheFailure, ValidationFailure, AuthFailure estendendo Equatable. injection.dart registra get_it lazy singletons (DatabaseHelper placeholder nesta task).</action>
  <verify>flutter pub get && flutter analyze --no-fatal-infos</verify>
  <acceptance_criteria>
    - pubspec.yaml contém flutter_bloc, sqflite, get_it, go_router, google_fonts
    - lib/main.dart contém configureDependencies e runApp
    - Diretórios lib/features/auth e lib/core/database existem
  </acceptance_criteria>
  <done>Projeto Flutter compila estruturalmente</done>
</task>

<task type="auto">
  <name>Task 2: Implementar tema Material 3 conforme UI-SPEC</name>
  <files>lib/core/theme/app_colors.dart, lib/core/theme/app_spacing.dart, lib/core/theme/app_typography.dart, lib/core/theme/app_theme.dart, lib/app.dart</files>
  <read_first>.planning/phases/01-foundation-auth/01-UI-SPEC.md</read_first>
  <action>Criar tokens: AppColors com dominant #F1F5F9, secondary #FFFFFF, accent #0F766E, destructive #DC2626, textPrimary #0F172A, textSecondary #64748B, border #E2E8F0. AppSpacing com xs=4, sm=8, md=16, lg=24, xl=32, xxl=48, xxxl=64. AppTypography com label 14/600, body 16/400, heading 20/600, display 28/600 usando Google Fonts Inter. app_theme.dart exporta ThemeData light com ColorScheme.fromSeed(seedColor: accent), scaffoldBackgroundColor dominant, inputDecorationTheme outlined com border #E2E8F0, filledButtonTheme height 48. PraJaApp usa MaterialApp.router com theme, locale pt_BR, supportedLocales [Locale('pt','BR')].</action>
  <verify>flutter analyze lib/core/theme/ lib/app.dart</verify>
  <acceptance_criteria>
    - app_colors.dart contém 0xFF0F766E
    - app_theme.dart exporta lightTheme ou appTheme
    - app.dart usa MaterialApp.router com theme definido
  </acceptance_criteria>
  <done>Tema UI-SPEC aplicado globalmente</done>
</task>

<task type="auto">
  <name>Task 3: SQLite, migrations e seed de prestadores mockados</name>
  <files>lib/core/database/database_helper.dart, lib/core/database/migrations.dart, lib/core/database/seed_data.dart, lib/core/di/injection.dart</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>DatabaseHelper singleton: openDatabase path join(await getDatabasesPath(), 'praja.db'), version 1, onCreate roda migrations. Tabelas v1: users (id TEXT PK, email TEXT UNIQUE, password_hash TEXT, name TEXT, cpf TEXT, birth_date TEXT, phone TEXT, user_type TEXT CHECK client/provider, created_at TEXT); provider_profiles (user_id TEXT PK FK, profession TEXT, specialty TEXT, address TEXT, description TEXT, is_complete INTEGER DEFAULT 0). migrations.dart centraliza CREATE TABLE statements. seed_data.dart: função seedMockProviders(Database db) insere 5 prestadores mockados (emails mock1@praja.local … mock5@praja.local) com perfis completos e especialidades variadas (Eletricista, Encanador, Pintor, Diarista, Técnico de informática). Usar flag shared_preferences key 'db_seeded' para rodar seed uma vez. Registrar DatabaseHelper em get_it; initDatabase() chamado em configureDependencies antes de runApp.</action>
  <verify>flutter test test/core/database/seed_data_test.dart 2>/dev/null || flutter analyze lib/core/database/</verify>
  <acceptance_criteria>
    - database_helper.dart contém openDatabase e onCreate
    - seed_data.dart contém pelo menos 5 prestadores mock com specialty distintas
    - migrations.dart define tabelas users e provider_profiles
  </acceptance_criteria>
  <done>SQLite persiste e seed mock executa no first launch</done>
</task>

</tasks>

<verification>
- [ ] flutter pub get succeeds
- [ ] flutter analyze passes
- [ ] flutter run launches app shell (router pode apontar placeholder)
</verification>

<success_criteria>
- All tasks completed
- DATA-01 and DATA-02 foundation ready
- Theme matches UI-SPEC tokens
- 5 mock providers seeded on first launch
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
